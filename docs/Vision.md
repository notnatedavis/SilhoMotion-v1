# Vision

- option triggers new pop up window containing draw output to project (settings for this physics sim would be adjusted in the main window and changes would be reflected on the output)
- the 'Launch Simulation'
    - opens a separate window with transparent background (or black ?)
    - settings applied are reflected in real time